const database = 'ctmd'
const username = 'ctmd_user'
const password = 'manager'
const host = 'localhost'
const port = 5432

module.exports = {
    database,
    username,
    password,
    host,
    port
}